const ForgotPasswordLayout = ({ children }: { children: React.ReactNode }) => {
  return (
    <div className="min-h-[80vh] lg:p-0 p-4 flex items-center justify-center">
      {children}
    </div>
  );
};

export default ForgotPasswordLayout;
