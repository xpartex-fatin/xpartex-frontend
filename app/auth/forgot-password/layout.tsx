const ForgotPasswordLayout = ({ children }: { children: React.ReactNode }) => {
  return (
    <div className="min-h-[90vh] mt-0 lg:mt-18 lg:p-0 p-4 flex items-center justify-center">
      {children}
    </div>
  );
};

export default ForgotPasswordLayout;
